# Migration Foreman — Frontend

Next.js 14 (App Router) + TypeScript + Tailwind, scaffolded via `create-next-app`. See [../docs/PROJECT.md](../docs/PROJECT.md) for architecture, contracts, and ownership.

## Getting Started

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Planned dependencies (not yet installed)

- **React Flow** — dependency graph visualization (seam review + live campaign view)
- **Recharts** — summary stats (campaign summary view)

Leave these installs to whoever starts frontend build work (see PROJECT.md section 9, Arushi).
